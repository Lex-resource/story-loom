# A43 V57 Compact Character Surface

- Parent: `A42 / V56`
- Prompt version: `V57`
- Variant: `ariadne-a43-v57-compact-character-surface`
- Status: partial runtime; excluded from ranking
- Extra default model calls: `0`

## Hypothesis

V56's `character_turn` did not sufficiently improve character portrayal because
the runtime still rendered the accumulated V45-V56 instruction chain. V57
tests whether one compact role-specific surface produces less procedural prose
while preserving V55/V56 continuity and authority behavior.

## Controlled change

V57 keeps the structured `primary_action` and `character_turn` fields and all
layered-memory behavior. For each Agent, the V54 dispatch now selects a single
compact V57 hint instead of recursively concatenating the older narrative and
audit hints. The change is prompt-only and adds no model call, schema, or
database migration.

## Verification

- CodeGraph synchronized before edits.
- V57-focused tests passed with the surrounding V55/V56 prompt tests (`36 passed`).
- A42-r2 was paused after chapters 2 and 3 completed; its raw artifacts remain
  unchanged and excluded from formal ranking.

## Runtime result

- The first A43 launch was excluded because the Worker was stale and the
  Writer snapshot lacked the V57 marker.
- Clean r2 project: `1ffa8571-f236-4ef6-a9f4-30f6adb66723`.
- Run directory: `data/research_runs/ariadne-a43-v57-compact-character-surface-r2-9ch-20260816/`.
- Chapter 2 reached `8.0/10` with `0/0/0` content/API/JSON retries.
- Planner emitted empty `primary_action` and `character_turn`, and the Writer
  returned to a procedural query/record/wait loop. The run was paused before
  chapter 3 and is excluded from ranking. Raw artifacts remain unchanged.

## Runtime gate

Run a fresh 10-chapter sample from the fixed chapter-1 seed with OpenAI as the
sole provider, backup and hybrid recall disabled, local Chroma embeddings, and
a new research directory. Do not reuse A42 memory or正文.
