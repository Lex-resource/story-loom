# A45 V59 Action-First Realization

- Parent: `A44 / V58`
- Prompt version: `V59`
- Variant: `ariadne-a45-v59-action-first-realization`
- Status: implementation complete; runtime pending
- Extra default model calls: `0`

## Hypothesis

V58 made the shared character turn non-empty, but the resulting prose can still
lead with interface operations and retrospective explanation. V59 keeps the
same structured turn and asks every stage to realize it in action-first order:
pressure, choice, immediate consequence, one necessary reveal, and a new
state. This isolates narrative sequencing from memory and retry changes.

## Evidence carried forward

A44 chapter 2 reached `8.71/10` with `0` content retries, `1` API retry, and
`6` successful LLM calls. It is retained as a valid partial runtime observation,
but it does not establish ten-chapter stability. Its raw events and prompts
remain under `data/research_runs/ariadne-a44-v58-structured-character-turn-9ch-20260816/`.

## Runtime gate

Start from the fixed chapter-1 seed in a fresh project and memory namespace.
Use OpenAI / `gpt-5.6-luna` only, disabled backup and hybrid recall, local Chroma
embeddings, and `bootstrap_skeleton=false`. Verify V59 markers and non-empty
`primary_action`/`character_turn` in the first Planner and Writer snapshots
before continuing chapters 2-10.

The first launch exposed an implementation gap: the V59 replacement Planner
hint omitted V58's object-shape requirement, so the model returned string
values for both turn fields and the schema gate correctly rejected the outline.
That launch is retained as an excluded startup-failure sample. V59 was repaired
to restate the fixed object fields before the clean A45-r2 launch.
