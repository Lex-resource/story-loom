# A47 V61 State Progression Closure

- Parent: `A46 / V60`
- Prompt version: `V61`
- Variant: `ariadne-a47-v61-state-progression`
- Status: implementation complete; runtime pending
- Extra default model calls: `0`

## Hypothesis

V60 made external consequences visible, but chapter 3 still repeated device-limit explanations and was paused because a generated observation of the same world object was treated as a high-risk replacement. V61 tests whether a shared state-progression surface plus a narrow `rule`/`confirmed` type-alias correction improves narrative closure and avoids false conflict review without increasing calls.

## Controlled variables

Unchanged: V60 structured turn and external-consequence rules, layered-memory authority, OpenAI-only routing, local Chroma embeddings, disabled backup and hybrid recall, retry policy, and no-extra-call design.

Changed: role-specific V61 hints and the deterministic world-rule alias handling in atom conflict review. Only `rule` and legacy `confirmed` are equivalent; other stable marker changes remain blocking.

## Runtime gate

Use a fresh project and memory namespace from the fixed published chapter-1 seed. Verify V61 markers and non-empty `primary_action`/`character_turn` in the first Planner and Writer snapshots before continuing chapters 2-10. Preserve A46's partial run and conflict artifacts unchanged.
