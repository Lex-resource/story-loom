# A41 V55 Primary Action Turn

- Parent: `A40 / V54`
- Prompt version: `V55`
- Variant: `ariadne-a41-v55-primary-action-turn`
- Status: partial runtime; still running; excluded from formal ten-chapter ranking until complete
- Extra default model calls: `0`

## Hypothesis

V54 constrained repeated operations but still allowed a procedural
query/confirmation/explanation loop. A single shared `primary_action` should
make the character's decision and cost the chapter's narrative turn, reducing
mechanical prose and downstream retry pressure.

## Controlled change

The contract normalizes one `primary_action` object with `action`, `response`,
`decision`, `cost_or_risk`, and `new_stage`. Planner, Writer, Editor, Validator,
and Extractor receive the same projection. V55 inherits V53's completed-event
and previous-terminal-state boundary. V54's compatibility field remains
accepted but is no longer the main prompt surface.

## Verification

- V55/V54/V53/Planner focused tests: pending full regression; initial focused
  result `32 passed`.
- Runtime controls: OpenAI-only, backup disabled, hybrid recall disabled,
  local Chroma embedding, fresh project and memory namespace.
- Raw runtime artifacts: pending under
  `data/research_runs/ariadne-a41-v55-primary-action-turn-10ch-20260816/`.

## Partial runtime observation

Chapters 2-6 have published at `8.57`, `8.57`, `8.71`, `8.57`, and `8.71`,
for a current average of `8.628/10`. Chapter continuity is `9/10` in all
five chapters. Content retries are `0` in every chapter; API retries are
`0, 1, 0, 0, 0`; JSON recoveries are all `0`. The five chapters used `32`
LLM calls in `1119.939s` summed wall-clock time and consumed `232747/51517`
input/output tokens. Chapter 7 is running. This is an interim observation,
not a final ranking result.
