# A37 V51 Character Agency And Observable Choice

- Parent: `A36 / V50`
- Prompt version: `V51`
- Variant: `ariadne-a37-v51-character-agency`
- Status: first attempt excluded by auxiliary prompt-gate failure; clean r2 pending
- First project: `aea4b0b7-07b1-4f59-95cf-045156add124`
- First run directory: `data/research_runs/ariadne-a37-v51-character-agency-10ch-20260816/`
- Provider: `OpenAI / gpt-5.6-luna`; backup provider disabled
- Embedding: `chroma_local`; hybrid recall disabled

## Hypothesis

A36/V50 removed content retries in its first three completed chapters, but its
average quality was only `8.43/10`, with character portrayal and writing still
limited. V51 keeps the V50 observation boundary and adds one character choice
grounded in existing card constraints or accepted state, rendered through a
visible tradeoff, risk, position, or direction change.

## Controlled change

V51 changes only the shared prompt surface. Planner selects one grounded key
choice; Writer renders it through action rather than psychological summary;
Editor compresses repeated procedure; Validator treats thin characterization
as a warning; Extractor stores observed choices only. Hard facts, unknown
boundaries, memory authority, local observation repair, retry classification,
and model-call count remain unchanged. Auxiliary Editor/Extractor prompts
must carry the same V51/V50 markers.

## First attempt and exclusion

The first project started from the fixed published chapter-1 seed with an
empty layered-memory namespace. Chapter 2 completed at `8.71/10` with
`0/1/0` content/API/JSON retries, 9 LLM attempts, and `338.663s` wall clock.
Continuity and foreshadowing both scored `9`; character portrayal and writing
scored `8`. The result is excluded because Editor `destyle` and Extractor
`summarize_change_candidates` snapshots did not include the V51/V50 markers;
the optional character-card extraction branch also had the same gap and
reported a recursion-limit skip. No A37-r1 chapter is reused by r2.

## Clean r2 gate

After the auxiliary prompt-surface fix, r2 must start from the same seed in a
new project and run directory. The first snapshots for Planner, Writer,
Editor review/destyle/force-revise, Validator, Extractor summarize/extract,
and character-card extraction must contain both `V51 角色选择落地` and
`V50 最小证据修复` where the branch is rendered. Only after that gate passes
does r2 qualify for the ten-chapter comparison.
