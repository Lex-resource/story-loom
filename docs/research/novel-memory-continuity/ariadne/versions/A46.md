# A46 V60 External Consequence

- Parent: `A45 / V59`
- Prompt version: `V60`
- Variant: `ariadne-a46-v60-external-consequence`
- Status: partial runtime; chapters 3-10 running
- Extra default model calls: `0`

## Hypothesis

V59 orders the turn as pressure, choice, immediate consequence, one necessary
reveal, and new state, but the consequence can still be only a device response.
V60 keeps that ordering and adds a stricter boundary: the choice must visibly
change the scene outside the interface, and the chapter must end in a concrete
state that cannot be reset by repeating the same query.

## Controlled variables

Unchanged: structured `primary_action` and `character_turn`, layered-memory
recall and authority rules, OpenAI-only routing, local Chroma embeddings,
disabled backup and hybrid recall, retry policy, and no-extra-call design.

Changed: role-specific prompt hints for Planner, Writer, Editor, Validator, and
Extractor. V60 is additive to V59 and does not modify the archived V59 helper.

## Runtime gate

Use a fresh project and memory namespace from the fixed published chapter-1
seed. Verify V60 markers and non-empty `primary_action`/`character_turn` in the
first Planner and Writer snapshots before continuing chapters 2-10. Preserve
the stopped A45-r2 artifacts as an incomplete, excluded observation.

## Partial runtime observation

The clean A46 run uses project
`1bc6aa0f-4da5-4558-8e2e-37c0236ba118` and run
`ariadne-a46-v60-external-consequence-9ch-20260816/`. The startup gate passed:
the bootstrap event is `not_needed`, the Planner and Writer snapshots contain
`V60`, and the Writer receives the non-empty shared turn and chapter-1 state.

Chapter 2 completed at `8.71/10` with dimensions `9/8/9/8/9/9/9`. It used
`6` successful calls and `7` attempts, with `0` content retries, `1` API rate
limit retry, and `0` JSON recoveries. Wall-clock time was `413.411s`; input and
output tokens were `64033/13068`, and average memory context was `4087`
characters. The generated scene visibly changed the character's position and
investigation direction, but the quality review still noted repeated device
and receipt-limit explanation. Chapters 3-10 remain in progress, so this is
not a ten-chapter ranking result.
