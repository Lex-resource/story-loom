# A44 V58 Structured Character Turn

- Parent: `A43 / V57`
- Prompt version: `V58`
- Variant: `ariadne-a44-v58-structured-character-turn`
- Status: implementation complete; runtime pending
- Extra default model calls: `0`

## Hypothesis

V57 successfully reduced the accumulated prompt surface, but its first clean
chapter omitted both `primary_action` and `character_turn`. The Writer then
returned to a procedural question/record/wait loop and chapter 2 scored
`8.0/10`, with character portrayal `7/10`.

V58 makes the shared turn explicit and deterministically fills only missing
fields from existing outline evidence. The expected improvement is a visible
choice and cost without weakening authority boundaries or increasing calls.

## V57 partial runtime evidence

- First A43 launch was excluded because the Worker was stale and rendered no
  V57 marker.
- Clean A43-r2 project: `1ffa8571-f236-4ef6-a9f4-30f6adb66723`.
- Run: `ariadne-a43-v57-compact-character-surface-r2-9ch-20260816`.
- Chapter 2 completed evaluation at `8.0/10`; dimensions were plot `8`,
  character `7`, world `8`, writing `8`, logic `8`, continuity `9`, and
  foreshadowing `8`. Content/API/JSON retries were `0/0/0`.
- Planner output had empty `primary_action` and `character_turn`; the run was
  paused and excluded from ranking. Raw prompts and responses remain intact.

## Runtime gate

Run V58 from a fresh copy of the fixed published chapter-1 seed. Verify the
first Planner snapshot and normalized Writer contract contain non-empty
`primary_action` and `character_turn` before allowing the batch to continue.
