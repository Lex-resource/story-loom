# A36 V50 Minimal Observation Patch

- Parent: `A35 / V49`
- Prompt version: `V50`
- Variant: `ariadne-A36-v50-minimal-observation-patch`
- Status: frozen after chapter 4; partial runtime observation, excluded from ten-chapter ranking
- Project: `b4027c5c-1053-426e-a3ca-85376547f9a3`
- Run directory: `data/research_runs/ariadne-a36-v50-minimal-observation-patch-10ch-20260816/`
- Provider: `OpenAI / gpt-5.6-luna`; backup provider disabled
- Embedding: `chroma_local`; hybrid recall disabled
- Seed: fixed published chapter 1 from project `707eb904-28cb-454b-9dd1-89ff2433de46`
- Expected extra default model calls: `0`; one optional Editor repair remains

## Hypothesis

V49 correctly classified the narrow observation-to-history/identity/causality
problem and successfully avoided Writer retries in A35 chapters 2-3. Its
repair call still used the generic force-revision surface, however. V50 adds a
minimal sentence-patch contract to all five Agents and explicitly injects it
into `EditorAgent.force_revise_chapter`. The Editor receives the Validator's
original evidence, unknown boundary, and replacement suggestion, and must
preserve all non-target prose.

## Hard-boundary protection

The V50 classifier is only enabled for V49's pure observation-language issue
class. Accepted-fact, timeline, location, item, character-state, core-event,
and mixed issue sets remain on the existing Writer-retry or terminal-block
path. A successful local patch is recorded as
`observation_language_minimal_patch` and does not increment content retries.

## Startup gate

The database seed copy contains only the published chapter 1, its initial
character card, read-only manifest/snapshot state, and an empty new layered
memory namespace. The Job starts at chapter 2 with `bootstrap_skeleton=false`.
The complete manifest records the provider, seed, backup setting, and runtime
controls. The first rendered prompt must contain `V50 最小证据修复` for every
writing Agent before the run is accepted.

## Partial runtime observation

The startup gate passed. Chapters 2-4 have passed through generation so far:

- Chapter 2: `8.14/10`, `0/0/0` content/API/JSON retries, `6` LLM calls,
  `172.030s`.
- Chapter 3: `8.57/10`, `0/0/0` content/API/JSON retries, `6` LLM calls,
  `172.111s`.
- Chapter 4: quality event `8.57/10`; no content/API/JSON retry event has
  appeared, and Extractor post-processing is still closing the chapter.

No `observation_language_minimal_patch` event appeared in chapters 2-4, so the
optional repair path was not exercised. The first three completed chapters
averaged `8.43/10`; character portrayal and writing quality remained the
limiting dimensions. The Worker was stopped before chapter 5 so the next
controlled prompt variant could start from the same seed. A36 is retained as
a valid partial observation but excluded from the ten-chapter ranking.
