# V16 确定性契约卫生层

## 假设

在 V15 的共享章节契约上增加不增加模型调用的确定性清洗，先修正 Planner 输出中可机械判定的数值矛盾，并把违反 `unknown` 地点边界的实体描述降级为未知来源描述。

## 实验

运行目录：`data/research_runs/v16-openai-tidal-20260813-contract-hygiene-formal/`

项目：`dc7c21cc-5b42-4dc9-b0d9-342e3bd6e22c`

供应商为 `OpenAI`，模型为 `gpt-5.6-luna`，备用 provider 关闭，embedding 使用 Chroma 本地 embedding。

第 1 章完成：正文约 4218 字，内容重试 0 次，API/上游重试 1 次，墙钟约 665 秒。V15 暴露的潮位数值矛盾和继电器未知地点矛盾没有再进入下游 Agent。

第 2 章暂停：正文先后触发物品访问交接冲突和相对时间算术冲突。第二个冲突是 `03:24:50` 开始、三十五秒后与 `03:25:00` 刷新同时成立，实际应为十秒后；3 次定点内容重试后仍未通过 Validator。

## 结论

V16 证明数值与 unknown 地点卫生层有效，但没有覆盖相对时间与绝对时间的换算，也没有覆盖跨章节物品持有/访问状态。V16 不作为完整十章成功样本，原始事件、Prompt 和正文全部保留。
