# 依赖钉版的**手写源**是 pyproject.toml [project].dependencies;uv.lock 是安装事实源。
# 本文件仅用于无 uv 的最小安装场景(pip install -r requirements.txt),
# 更新依赖时改 pyproject 后执行 `uv export --format requirements-txt > requirements.txt`。
fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.14.0
chromadb==0.5.0
httpx==0.27.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1
json-repair>=0.30.0
tiktoken==0.7.0
numpy==1.26.4
